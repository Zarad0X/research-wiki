# Raw Sources

这里放原始资料。

建议：

- 网页转 markdown 后直接放这里
- 论文、访谈、播客笔记、会议纪要都可以
- 尽量一份 source 一个文件
- 文件名用日期或简短 slug
- 已经进入研究库、值得长期保留的原始材料再放进来
- 这层尽量稳定，不要频繁改写
- 可以和 `wiki/papers/` 或 `wiki/sources/` 页面的 `Sources` 段互相对应

这层是 source of truth，agent 读取但不改写原始内容。
